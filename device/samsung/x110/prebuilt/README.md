# Place extracted prebuilt files here:
#   kernel   — from stock boot.img (unpack_bootimg)
#   dtb.img  — from stock boot.img (unpack_bootimg)
#   dtbo.img — from dtbo partition (adb pull /dev/block/by-name/dtbo dtbo.img)
#
# These files are device-specific and cannot be distributed with the source tree.
